"use client"
import React, {useState} from 'react'
import { useStore } from '@/store'
import UploadForm from './UploadForm'
import Image from 'next/image'

export default function Result() {
  const imageResult = useStore((state)=>[state.imageResult])

  return (
    
     
    <div className='flex items-center flex-col py-3 p-4'>
      {
        !imageResult[0] ? 
            // if no result render these
          ''
        :
          // if has result render these
        <>
          <Image 
            src={imageResult[0]}
            alt="Image Generated by AI" 
            width={500} 
            height={500}
            className='border-2 border-gray-300 rounded-lg shadow-md'
          />
           <UploadForm/>
        </>
        
      }
    </div>
  
  )
}
